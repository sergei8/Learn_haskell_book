- ссылочная прозрачность это свойство функции возвращают всегда одно и тоже значение
ссылочная прозрачность определяет следующие свойства языка его ленивость - функция будет вызвана только тогда когда она действительно нужна

Применение функции это вызов функции с её параметрами. Операция имеет наивысший приоритет

Константное функция эта функция которые не принимают аргументов

Генератор списков состоит из производящей функции переменной И ограничения

